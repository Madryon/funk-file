# Copy to .env for local development. Never commit real keys.
MAX_UPLOAD_MB=50

# Keep false for fast local processing. Set true to try iLovePDF first.
USE_ILOVEPDF=false

# Add these only when you have iLovePDF developer credentials.
ILOVEPDF_PUBLIC_KEY=
ILOVEPDF_SECRET_KEY=
